// cart routing ====================

function cartRoutes(app) {
  //   app.post("/cart", cartController);
  //   app.put("/cart/:id", cartController);
  //   app.delete("/cart/:id", cartController);
}

module.exports = cartRoutes;
